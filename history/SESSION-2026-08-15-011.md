# SESSION — 2026-08-15 — Missão 4: auditoria read-only de sudo e LXD

## Objetivo e autorização

LEANDRO autorizou conexão à VPS exclusivamente para auditoria read-only de sudo/NOPASSWD e do privilégio LXD da conta `ubuntu`. Não houve autorização para alterar sudo, LXD, root, SSH, firewall, usuários, pacotes, serviços ou qualquer configuração.

## Cronologia

1. Uma primeira tentativa de autenticação não foi concluída com sucesso e terminou sem efeito operacional.
2. A coleta posterior autenticou como `ubuntu` e confirmou usuário `ubuntu`, UID/GID `1000`, home `/home/ubuntu`, hostname `vmi3506102` e grupos `ubuntu adm cdrom sudo dip lxd`.
3. `sudo -n -l` terminou com exit code `0` e confirmou política efetiva com acesso geral e `NOPASSWD`.
4. A elevação não interativa confirmou UID `0` e usuário `root`, sem alteração remota.
5. A origem sudo foi identificada como `/etc/sudoers.d/90-cloud-init-users`, proprietário `root:root`, modo `440`, com regras ativas `ubuntu ALL=(ALL) NOPASSWD:ALL` e `root ALL=(ALL) NOPASSWD:ALL`.
6. `visudo -cf /etc/sudoers` validou a configuração com resultado `PASS`.
7. O LXD observado era `5.21.6-78b046a`, revisão `40361`; seu socket existia com proprietário `root:lxd`, modo `660` e era gravável por `ubuntu`.
8. A associação de `ubuntu` ao grupo `lxd` e a capacidade de escrever no socket confirmaram um caminho de privilégio equivalente a root. Nenhum comando `lxc`, chamada à API ou exploração foi executado.
9. O daemon LXD permaneceu `inactive/dead` antes e depois; a socket unit permaneceu `active/listening/enabled` antes e depois.
10. Os hashes de listeners antes e depois foram idênticos: `bbda5db2de8957b27e25815cd797a72b67a5e11fe14bbe4a06ff7272f362383b`.
11. A auditoria encerrou sem escrita de configuração, mudança de serviço, alteração de firewall ou qualquer mudança operacional.

## Findings resultantes

- `FND-LXD-001` permanece **OPEN/HIGH**, fortalecido por evidência direta do socket gravável e do caminho equivalente a root.
- `FND-SUDO-001` foi registrado como **CONFIRMED — OPEN/HIGH** para documentar a elevação direta e não interativa a root.
- Nenhum finding anterior foi resolvido por esta coleta.

## Próximo ponto

O próximo micro-passo recomendado é uma revisão read-only de recovery proporcional e dos caminhos de recuperação, sujeita a novo HUMAN_GATE. Root/senha permanece validado, temporário e preservado; Cloud Workstation continua `PRIORITY_PLANNED` após os pré-requisitos mínimos da Fase 1.

## Controles de encerramento

- `AUDIT_READ_ONLY=YES`
- `SUDO_CONFIGURATION_CHANGED=NO`
- `LXD_CONFIGURATION_CHANGED=NO`
- `LXC_COMMAND_EXECUTED=NO`
- `SERVICE_CHANGE_EXECUTED=NO`
- `FIREWALL_CHANGED=NO`
- `OPERATIONAL_CHANGE_EXECUTED=NO`
