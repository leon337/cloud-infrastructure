# SESSION — 2026-08-14 — Coleta técnica do inventário da VPS

## Objetivo

Concluir a coleta técnica da Etapa 0.5 — Inventário real da VPS — usando apenas comandos de leitura e sem realizar mudanças estruturais.

## Estado inicial

- FASE 0 — ORIENTAÇÃO E INVENTÁRIO.
- Etapa 0.5 — em andamento.
- PUC v1.0 validado com CONTINUIDADE COMPLETA.
- `FND-SSH-001`: RESOLVED.
- Keepalive permanente do cliente SSH local: aplicado e validado.
- Próximo passo inicial da sessão: armazenamento/filesystems.

## Comandos de inventário executados na VPS REMOTA

Somente leitura:

- `lsblk`;
- `lsblk -f`;
- `df -hT`;
- `fdisk -l /dev/sda`;
- `findmnt -o TARGET,SOURCE,FSTYPE,OPTIONS`;
- `ip -brief address`;
- `ip route`;
- `ip -6 route`;
- `resolvectl status`;
- `ss -lntup`;
- `uptime`;
- `timedatectl`;
- `systemctl --failed --no-pager`;
- `systemctl is-system-running`;
- `apt list --upgradable`.

Houve tentativas de digitação inválidas sem efeito:

- `DF -Ht` / `DF -HT`: comando inexistente por uso de maiúsculas;
- `fdisk -l/dev/sda`: opção inválida por ausência do espaço; depois corrigido para `fdisk -l /dev/sda`.

Nenhuma dessas tentativas alterou a VPS.

## Principais fatos coletados

### Armazenamento

- `/dev/sda`: QEMU HARDDISK, 300 GiB, GPT.
- `/dev/sda1`: ~299G, ext4, `cloudimg-rootfs`, montado em `/`.
- Raiz via `df -hT`: ~290G total, ~2.4G usados, ~288G disponíveis, ~1% de uso.
- `/dev/sda14`: 4M, BIOS boot.
- `/dev/sda15`: ~106M, vfat/FAT32, EFI System, `/boot/efi`.
- `/dev/sda16`: ~913M, ext4, `/boot`.
- `sr0`: ROM virtual ~4M, iso9660, label `cidata`.

### Rede

- `eth0`: UP.
- IPv4: `169.58.171.192/17`.
- Gateway IPv4: `169.58.128.1`.
- IPv6 global: `2a02:c207:2350:6102::1/64`.
- Gateway IPv6: `fe80::1`.
- DNS: `195.179.224.53` e `209.126.15.53` via `systemd-resolved` stub.
- `sshd`: TCP `0.0.0.0:22` e `[::]:22`.
- DNS local do `systemd-resolved`: `127.0.0.53:53` e `127.0.0.54:53`.

### Estado básico

- uptime observado: ~5h37.
- load average: `0.00 0.03 0.00`.
- timezone: `Europe/Berlin`, CEST/UTC+2 no momento observado.
- NTP ativo e relógio sincronizado.
- unidades systemd falhas: 0.
- estado geral do systemd: `running`.
- `apt list --upgradable`: cinco pacotes krb5 listados como atualizáveis com base nos índices APT existentes.
- nenhum `apt update` foi executado, portanto a lista de updates é observação dos índices locais, não garantia de estado atual dos repositórios externos.

## Mudanças realizadas

Nenhuma mudança operacional ou estrutural na VPS.

Não foram executados:

- `apt update`/`apt upgrade`;
- firewall;
- particionamento;
- alteração de filesystem;
- alteração de mounts;
- alteração de rede/DNS;
- alteração de timezone;
- alteração de swap;
- Docker;
- desktop gráfico;
- hardening.

## Delta canônico

- `docs/06-inventario.md`: coleta técnica completa consolidada;
- `docs/05-roadmap.md`: Etapa 0.5 permanece IN_PROGRESS, agora aguardando fechamento didático;
- `state/current.yaml`: armazenamento, rede e estado básico incorporados;
- `CHECKPOINT.md`: ponto de retomada movido para revisão consolidada com LEANDRO;
- `CONTEXT.md`: estado resumido atualizado;
- este histórico criado.

## Estado final

- coleta técnica da Etapa 0.5: **CONCLUÍDA**;
- Etapa 0.5: ainda **IN_PROGRESS**;
- motivo: a Definition of Done didática exige revisão/entendimento de LEANDRO antes de marcar `DONE`;
- FASE 1: ainda não iniciada;
- próximo passo exato: revisar com LEANDRO o inventário consolidado e confirmar entendimento.
