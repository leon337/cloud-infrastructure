# SESSION 2026-08-19-019 — F1.2c exact-head validation resume

## Objetivo

Retomar a trilha principal F1.2c sem tocar no NODE-01 até satisfazer novamente o gate commit-bound do HEAD exato.

## Estado recuperado

- branch: `codex/mission-001-f1-2c-network-enforcement`;
- último commit material de network services com CI verde: `f771cfd09f1824562ddfdaea507fb3cb0781f6ac`;
- delta posterior até `b4cdc9d05c70d815621872f5f526ff458d8fd544`: somente documentação, state, evidence e history; nenhum código de aplicação;
- PR draft de validação: `#6`;
- PR #6 possui conflito com `main`, portanto o caminho `pull_request` não foi usado como prova de CI;
- workflows `foundation-ci` e `docker-boundary-ci` aceitam `push` em `codex/**`.

## Decisão

Criar este commit documental para disparar os dois workflows por `push` no HEAD exato da branch, sem rebase/merge e sem alteração da VPS.

## Guardrails

- nenhum apply no NODE-01 antes do CI do HEAD exato;
- nenhum workload de usuário;
- produção não autorizada;
- Management Network fora deste slice;
- credential rotation continua `DEFERRED_BY_HUMAN_DECISION`;
- runner privilegiado temporário permanece sem reativação até os gates anteriores estarem satisfeitos.
