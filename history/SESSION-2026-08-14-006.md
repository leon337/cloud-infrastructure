# SESSION — 2026-08-14 — Início controlado da FASE 1

## Objetivo

Registrar a instrução explícita de LEANDRO para dar continuidade à missão e avançar para a próxima etapa/fase após o fechamento da FASE 0.

## Estado inicial

- FASE 0 — ORIENTAÇÃO E INVENTÁRIO: `DONE`.
- Etapas 0.1 a 0.5: `DONE`.
- FASE 1 — Base do sistema e segurança inicial: `PROVISIONAL_NOT_STARTED`.
- Nenhuma mudança de segurança da FASE 1 havia sido executada.

## Instrução de LEANDRO

LEANDRO determinou: **“VAMOS PARA A PROXIMA ETAPA”** e reforçou que a missão deve sempre manter continuidade.

Interpretação operacional:

- `AUTH-PHASE-1-START-001`;
- a FASE 1 pode ser iniciada formalmente;
- o início da fase não substitui HUMAN_GATEs específicos para mudanças de segurança, lockout, upgrade em lote ou mudanças estruturais.

## Estado resultante

- FASE 1 — Base do sistema e segurança inicial: `IN_PROGRESS`.
- Primeiro micro-passo planejado: atualização inicial.
- A atualização inicial começará por `apt update` para atualizar índices de pacotes sem instalar upgrades.
- `HG-F1-APT-UPDATE-001`: `PENDING`.

## Base para a decisão

O Plano Mestre define a seguinte sequência para a FASE 1:

1. atualizações iniciais;
2. usuário administrativo próprio;
3. sudo;
4. estratégia de SSH;
5. chave SSH;
6. validação de acesso por chave;
7. política de root;
8. política de senha SSH;
9. menor privilégio.

A política de segurança determina que nenhuma melhoria deve elevar o risco de perda de acesso sem recovery validado e que as mudanças devem ser pequenas e verificadas.

## O que foi alterado nesta sessão

Somente documentação canônica no GitHub para refletir a transição de fase.

## O que NÃO foi alterado

Nenhuma mudança operacional foi executada na VPS.

Não foram executados:

- `apt update`;
- `apt upgrade`;
- criação de usuário administrativo;
- alteração de sudo;
- criação/instalação de chave SSH;
- mudança da política de root;
- desativação de senha SSH;
- firewall;
- particionamento;
- swap;
- Docker;
- desktop gráfico;
- hardening em lote.

## Próximo passo exato

Apresentar a LEANDRO o primeiro micro-passo da FASE 1:

```bash
apt update
```

Explicar que ele atualiza os índices APT, não instala upgrades; apresentar risco e recovery; obter `HG-F1-APT-UPDATE-001`; somente se autorizado, executar o comando na VPS e analisar o resultado antes de qualquer `apt upgrade`.
