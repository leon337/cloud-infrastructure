# SESSION — 2026-08-14 — Validação permanente do keepalive SSH

## Objetivo

Concluir a correção permanente do `FND-SSH-001` no cliente SSH do Linux Mint local e validar que a sessão permanece funcional após ociosidade.

## Estado inicial

- FASE 0 — ORIENTAÇÃO E INVENTÁRIO.
- Etapa 0.5 — Inventário real da VPS.
- PUC v1.0 validado com CONTINUIDADE COMPLETA.
- Retomada operacional liberada.
- `FND-SSH-001`: mitigação temporária validada; correção permanente autorizada e ainda pendente.
- Estado local relatado anteriormente: `~/.ssh/config` inexistente.

## Procedimento

No Linux Mint LOCAL `leo@leo-N43SM`:

1. foi revalidada a identidade local;
2. foi revalidado que `~/.ssh/config` não existia;
3. o arquivo foi criado vazio;
4. a permissão foi definida como `600`;
5. foi adicionado o bloco:

```sshconfig
Host contabo-vps
    HostName 169.58.171.192
    User root
    ServerAliveInterval 30
    ServerAliveCountMax 3
```

6. `ssh -G contabo-vps` confirmou a configuração efetiva:
   - `user root`;
   - `hostname 169.58.171.192`;
   - `serveralivecountmax 3`;
   - `serveraliveinterval 30`;
7. `ssh contabo-vps` abriu corretamente a VPS e apresentou `root@vmi3506102`;
8. a sessão permaneceu ociosa por aproximadamente 3 minutos;
9. na mesma sessão foi executado `echo vivo`;
10. resultado: `vivo`.

## Resultado

**PASS.**

O keepalive permanente do cliente SSH local foi aplicado e validado no cenário que reproduzia o finding.

## Delta canônico

- `findings/FND-SSH-001.md`: marcado como `RESOLVED`;
- `state/current.yaml`: configuração local, alias e keepalive permanente registrados como validados;
- `CHECKPOINT.md`: próximo passo avançou para inventário de armazenamento/filesystems;
- `CONTEXT.md`: estado operacional resumido atualizado;
- este registro histórico criado.

## O que NÃO foi alterado

Nenhuma configuração da VPS foi modificada para resolver o finding. A mudança ocorreu apenas no cliente SSH do Linux Mint local.

Não foram executados:

- firewall;
- particionamento;
- swap;
- hardening;
- Docker;
- desktop gráfico;
- política de root/senha.

## Estado final

- `FND-SSH-001`: RESOLVED.
- Alias `contabo-vps`: validado.
- Keepalive permanente: aplicado e validado.
- Fase: FASE 0 — ORIENTAÇÃO E INVENTÁRIO.
- Etapa: 0.5 — em andamento.
- Próximo passo exato: continuar o inventário real da VPS por armazenamento/filesystems, com comandos somente leitura e explicação prévia.
