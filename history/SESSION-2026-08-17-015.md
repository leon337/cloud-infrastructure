# SESSION — 2026-08-17 — Conclusão repo-only Docker boundary F1.2b

## Escopo e recuperação

- branch `codex/mission-001-foundations-f1-2b-preparation` recuperado limpo em
  `d849caa0eafdc231d2782be602be1a2263758b7b`;
- Q1–Q39 preservadas; Q40-D usado somente para implementação incremental
  DEV/lab;
- nenhuma leitura de secrets, execução de `sudo`, instalação, playbook contra
  inventory/host, mutação de serviço/rede/firewall/SSH ou operação em produção;
- naquele checkpoint inicial F1.1 estava `PARTIAL/NOT_APPLIED`; o fechamento real
  posterior desta sessão o promoveu a `DONE` com evidência.

## Implementação

O commit local `7015c80759a797bcb141773b79cd9b95f6fbecf1` concluiu:

- role e playbooks de apply/rollback com target e allowlists imutáveis;
- payload APT pinado para os cinco pacotes exatos, chave/source/config/drop-ins;
- verificação do índice autenticado por versão, path Noble/stable e SHA-256;
- supressão de autostart e prova de ausência de daemon/state antes do start;
- socket root-only, grupo vazio, runtime sem bridge/workload/TCP/metrics;
- baseline e removal manifest das duas raízes literais com `find -xdev`,
  proteção de symlink/hardlink/mount/open-path/device/inode/escape;
- harness GitHub-hosted com check sem mutação, apply, idempotência, restart,
  invariância, recusas fail-closed, rollback e cleanup;
- CI com ShellCheck v0.11.0 pinado por SHA-256.

## Validação local permitida

- secret policy, links, YAML estrito, manifests e state cross-check: `PASS`;
- 55 testes unitários/negativos: `PASS`;
- sintaxe de seis shell scripts: `PASS`;
- ShellCheck v0.11.0 em seis scripts: `PASS`;
- Ansible Core 2.21.3, seis `--syntax-check`: `PASS`;
- `git diff --check`: `PASS`.

ShellCheck e Ansible foram baixados de artefatos oficiais, verificados por
SHA-256, extraídos somente em `/tmp` e removidos. Nenhum pacote foi instalado.
Nenhum lifecycle privilegiado foi executado; CI e VM descartável permanecem
`PENDING`.

## Limites e continuidade

- o helper teve comportamento dinâmico testado apenas sobre roots temporários
  não privilegiados;
- apply/rollback APT/systemd/Docker e invariância de ruleset ainda precisam da
  VM GitHub-hosted descartável;
- check/apply F1.2b permaneceu bloqueado até o fechamento real F1.1 abaixo;
- o primeiro container continua bloqueado por F1.2c;
- produção e rotação de credenciais continuam fora do escopo.

## Continuidade repo-only — F1.2c

O commit `b4cbeb066605754d538ff5abe2d294f0759d6f59` criou somente o contrato
machine-readable de network enforcement e quatro testes. Ele codifica Q20/Q34,
deny-by-default, IPv4/IPv6, zonas protegidas, grants, profiles e a matriz de
evidência obrigatória. A suíte integrada passou com 60 testes e 34 YAML.

Tecnologia, ADR, ruleset, DNS/egress, harness dinâmico e prova de conectividade
permanecem `UNRESOLVED`/`PENDING`. Nenhum workload, execução no NODE-01,
produção ou rotação foi autorizado.

## Continuidade real — preview F1.1

Em `2026-08-17T05:48:16Z`, o playbook F1.1 foi executado no NODE-01 somente com
`--check --diff` e sudo humano interativo. O recap foi `localhost ok=6
changed=0` e `node-01 ok=28 changed=4 failed=0 unreachable=0 skipped=22`.
As mudanças foram apenas simuladas; o hash sanitizado do log foi registrado em
`evidence/SLICE-001/baseline.yaml`.

A leitura posterior de `2026-08-17T05:52:15Z`, sem sudo e sem cliente `lxc`,
confirmou marker, conta, grupo, paths, tmpfiles, slices e lock ausentes; zero
units falhas; SSH/UFW/fail2ban/XRDP/LightDM ativos; LXD inativo; Docker e
containerd ausentes. Esse era o estado anterior ao apply real registrado abaixo.

## Fechamento real — F1.1 DONE

O backup mais recente passou checksum/listagem e foi copiado off-host com hash
correspondente em `2026-08-17T06:35:55Z`. O apply real terminou com `changed=7`,
`failed=0`; a segunda reconciliação teve `changed=0`, `failed=0`. A validação
privilegiada confirmou marker/proveniência, diretórios e arquivos com
owner/group/mode exatos, credenciais runtime `root:root 0700`, conta bloqueada
nologin e nenhum grupo privilegiado. A auditoria externa confirmou zero units
falhas, serviços essenciais invariantes, slices carregadas/vazias, lock ausente,
LXD inativo, Docker ausente e nenhum listener novo da plataforma.

F1.1 está `DONE`. O próximo passo permitido é somente check mode F1.2b; apply
Docker depende da reconciliação desse preview e o primeiro workload permanece
bloqueado por F1.2c.

## F1.2b — primeira recusa fail-closed

A primeira tentativa de preview F1.2b foi recusada no controller, antes de
contato remoto, devido à comparação entre inventário canonicalizado e repository
root não canonicalizado. O recap foi `changed=0`; nenhuma mutação ocorreu. A
correção passou a aplicar `realpath` também ao repository root, recebeu teste de
regressão e passou suíte estática e preflight DEV real sem sudo. O preview
privilegiado permanece pendente até CI do commit corretivo.
