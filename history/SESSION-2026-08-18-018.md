# SESSION-2026-08-18-018 — NODE-01 network-services desired state CI

Data: 2026-08-18
Escopo: repositório e GitHub Actions; nenhuma mutação na VPS.

## Resultado

- o desired state bounded dos serviços F1.2c foi concluído e publicado;
- quatro containers exatos foram declarados: CoreDNS e Squid para os escopos
  development-default e restricted-no-internet;
- não há porta publicada, shell arbitrário, segredo ou workload de usuário;
- três bridges internas e uma bridge de egress de infraestrutura são mediadas
  por chains próprias, deny-default e grants explícitos;
- o primeiro workload continua bloqueado.

## Evidência commit-bound

- commit: `f771cfd09f1824562ddfdaea507fb3cb0781f6ac`;
- Docker/network run: [`32131461110`](https://github.com/leon337/cloud-infrastructure/actions/runs/32131461110);
- foundation run: [`32131461088`](https://github.com/leon337/cloud-infrastructure/actions/runs/32131461088);
- validação estática: 39 YAML, dois manifests, 123 testes, 15 scripts em
  syntax/ShellCheck e seis syntax-checks Ansible;
- lifecycle NODE-01 descartável: apply `1`, idempotência `0`, DNS `PASS`, proxy
  `PASS`, egress direto negado, restart `PASS` e rollback limpo;
- regressões de foundation, Docker boundary, F1.2c base, redes internas e
  política de serviços passaram.

## Correções durante a prova

- configs não secretas montadas em containers non-root passaram a ser
  root-owned e world-readable (`0644`), sem write para o processo;
- um health check incompatível com `no-new-privileges` foi removido sem reduzir
  a prova de processo/serviço;
- o probe de proxy passou a usar uma requisição HTTP real permitida;
- o cleanup descartável passou a remover cada arquivo exato individualmente.

## Estado operacional e continuidade

- NODE-01 não foi alterado nesta sessão;
- base F1.2c real permanece ativa e runtime permanece sem workload;
- próximo passo: verificar por leitura o runner temporário e, se necessário,
  reativá-lo por bootstrap assinado em HUMAN_GATE antes do apply real;
- produção continua não autorizada;
- rotação de credenciais continua adiada;
- nenhuma senha exposta no chat foi usada ou persistida.
