# SESSION — 2026-08-14 — Validação dos upgrades iniciais e phased updates

## Objetivo

Registrar a validação posterior ao `apt update` na **FASE 1 — Base do sistema e segurança inicial**, sem instalar upgrades e sem contornar o mecanismo padrão de phased updates do Ubuntu.

## Estado inicial

- `apt update`: concluído com sucesso.
- índices APT: atualizados.
- nenhum pacote havia sido atualizado.
- mensagem pós-refresh: 5 pacotes atualizáveis.

## Validação 1 — lista de pacotes

Foi executado na VPS REMOTA:

```bash
apt list --upgradable
```

Resultado observado:

- `krb5-locales`;
- `libgssapi-krb5-2`;
- `libk5crypto3`;
- `libkrb5-3`;
- `libkrb5support0`.

Versão instalada observada: `1.20.1-6ubuntu2.7`.
Versão candidata observada: `1.20.1-6ubuntu2.8`.

## Validação 2 — simulação de upgrade

Uma primeira tentativa foi digitada incorretamente como:

```text
apt get -s upgrade
```

Resultado: `E: Invalid operation get`.

Essa tentativa não produziu mudança no sistema.

Em seguida foi executado corretamente:

```bash
apt-get -s upgrade
```

Resultado observado:

```text
The following upgrades have been deferred due to phasing:
  krb5-locales libgssapi-krb5-2 libk5crypto3 libkrb5-3 libkrb5support0
0 upgraded, 0 newly installed, 0 to remove and 5 not upgraded.
```

## Interpretação operacional

- a simulação foi concluída com sucesso;
- nenhum pacote foi instalado;
- nenhum pacote novo seria instalado pelo upgrade padrão naquele momento;
- nenhum pacote seria removido;
- os cinco pacotes foram explicitamente adiados pelo mecanismo de phasing do APT;
- não houve motivo operacional para contornar o phasing;
- nenhum override de phased updates foi executado.

## Estado resultante

O item `atualizações iniciais` da FASE 1 fica registrado como:

`COMPLETE_WITH_PHASED_UPDATES_DEFERRED`

Isto significa que a baseline APT foi atualizada e avaliada, mas os cinco updates candidatos permanecem adiados pela política padrão do Ubuntu.

A FASE 1 permanece `IN_PROGRESS`.

## Próximo item

**Usuário administrativo próprio.**

Antes de criar qualquer conta ou alterar `sudo`, o próximo micro-passo será somente leitura: inspecionar quais contas locais potencialmente humanas já existem na VPS.

Qualquer criação de usuário administrativo ou alteração de privilégios exigirá explicação prévia, risco/recovery e HUMAN_GATE aplicável.

## O que NÃO foi executado

- nenhum `apt upgrade` real;
- nenhum override de phased updates;
- nenhuma criação de usuário;
- nenhuma alteração de `sudo`;
- nenhuma mudança de SSH;
- nenhuma mudança de root;
- nenhum firewall;
- nenhuma alteração de disco/swap;
- nenhuma instalação de Docker ou GUI.
