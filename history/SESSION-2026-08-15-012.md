# SESSION — 2026-08-15 — Fase 1 e Cloud Workstation

## Escopo

LEANDRO autorizou execução autônoma end-to-end da segurança mínima, recovery proporcional, backup, Cloud Workstation, documentação, commits e push. Credenciais temporárias foram usadas apenas para autenticação e não foram registradas em arquivos versionados.

## Cronologia

1. A Missão 6A foi reconciliada: VNC configurado, Rescue disponível, snapshots ausentes, backup do provedor não contratado e firewall Contabo não configurado.
2. O VNC foi revalidado funcionalmente com TigerVNC e mostrou o console `tty1`.
3. Foi criado backup pré-hardening em `/root/recovery/pre-phase1-hardening-20260815`.
4. O SSH foi endurecido para `ubuntu`/publickey; root/senha/keyboard-interactive foram desativados e uma sessão independente passou.
5. UFW foi ativado com somente OpenSSH; fail2ban/sshd foi instalado e validado.
6. Após confirmar zero instâncias LXD, `ubuntu` saiu do grupo `lxd` e o snap foi desabilitado.
7. A regra cloud-init `NOPASSWD:ALL` saiu do conjunto ativo; `visudo`, falha de `sudo -n` e sudo autenticado foram validados.
8. Foram instalados XFCE, LightDM, XRDP/xorgxrdp, VS Code, Firefox e utilitários de desktop.
9. Uma substituição excessivamente ampla de linhas `port=` no `xrdp.ini` impediu o backend Xorg. O arquivo original foi restaurado e somente a porta global foi alterada para loopback.
10. O XFCE inicialmente falhou porque `/home/ubuntu/.config` fora criado como `root:root`. O proprietário foi corrigido somente nesse diretório; a sessão iniciou.
11. O Firefox Snap falhou no cgroup XRDP. Foi substituído pelo Firefox DEB oficial da Mozilla após verificação da chave do repositório.
12. Passaram desktop, navegador, VS Code, terminal, terminal integrado, Thunar, projeto Git, múltiplas janelas, clipboard bidirecional, resolução dinâmica, reconnect, persistência e logout/login.
13. NetworkManager e ModemManager foram desativados após confirmar `systemd-networkd`, endereço e rota ativos.
14. Cinco updates Krb5 foram aplicados; zero updates permaneceram.
15. Foi criado backup diário sanitizado. O primeiro disparo falhou porque o diretório protegido ainda não existia; o segundo porque `ufw status` tenta criar lock. O diretório foi pré-criado e a coleta efetiva passou a usar `iptables-save`.
16. O diretório protegido de backups ganhou criação persistente por `systemd-tmpfiles`; o serviço foi executado novamente com `Result=success` e o timer permaneceu ativo/habilitado.
17. O primeiro backup foi copiado off-host, teve SHA-256 remoto/local idêntico e extração de 24 arquivos validada.
18. Um reboot final confirmou SSH, UFW, fail2ban, XRDP, LightDM, backup, LXD inativo e Cloud Workstation; a leitura final com sessão gráfica ativa registrou 8 CPUs, ~2,2 GiB/23 GiB de RAM e ~7,5 GiB/290 GiB de disco usados.

## Resultado

- FASE 1: `DONE`;
- FASE 2: `DONE — FUNCTIONAL_AND_VALIDATED`;
- `FND-SSH-002`, `FND-LXD-001` e `FND-SUDO-001`: resolvidos;
- `FND-BACKUP-001`: mitigado e aberto;
- nenhum secret versionado;
- próximo passo: rotação das credenciais temporárias.

## Recursos finais

8 CPUs, ~2,2 GiB de 23 GiB RAM usados na leitura final com sessão gráfica ativa, ~7,5 GiB de 290 GiB de raiz usados, zero updates pendentes.
