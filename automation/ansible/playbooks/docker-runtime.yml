---
- import_playbook: docker-runtime-controller-preflight.yml

- name: Reconcile the empty root-only Docker boundary on DEV node-01
  hosts: platform_nodes
  gather_facts: true
  become: true
  serial: 1
  force_handlers: true

  roles:
    - role: docker_runtime
