---
- import_playbook: controller-preflight.yml

- name: Reconcile the Cloud Platform foundation on DEV node-01
  hosts: platform_nodes
  gather_facts: true
  become: true
  serial: 1
  force_handlers: true

  roles:
    - role: platform_foundation
