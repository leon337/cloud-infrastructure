schema_version: 1
project_id: cloud-infrastructure
purpose: Govern and reproduce the development and lab infrastructure that can host MCF agents without making historical host evidence current by implication.
lifecycle: ACTIVE
snapshot:
  current_workstream: control-bridge-context-reconciliation
  current_status: G2A_LOCAL_CONTEXT_ADAPTER_IMPLEMENTED_DISABLED_BY_DEFAULT_UNIT_PASS_E2E_PENDING__G2B_TASK8_LAB_VALIDATED_INACTIVE
  next_action: Run the disposable MCF-client to local G2-A adapter E2E with a repository fingerprint before considering adapter activation.
  blockers:
    - G2-A operational state is historical and requires live verification before material use.
    - The local G2-A Context adapter has unit and contract proof only; its disposable E2E and repository immutability proof are pending.
    - G2-B Tasks 9 and 10 are not started; real write, rollback, revocation, and MCF effective use are not proven.
    - G2-B activation, NODE-01 access, publication, merge, and production mutation are not authorized by this capsule.
sources:
  current_state: context/mcf-cloud-context.yaml
observed_at: "2026-08-23T04:19:46-03:00"
